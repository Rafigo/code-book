import React from 'react';
import Appbar from './packages/appbar';

const App = (): React.ReactElement => {
  return (
    <div className="App">
      <Appbar />
    </div>
  );
};

export default App;
